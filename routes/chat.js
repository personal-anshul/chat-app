/*
 * GET user info popup.
 */
exports.chatMsg = function(req, res) {
  // if(users.name) {
    // errorMessage = "";
    // res.render('chat', { title: 'Home' });
  // }
  // else {
  //   res.redirect('/');
  // }
}
